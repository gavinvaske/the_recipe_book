import React from 'react';
import { observer } from 'mobx-react-lite';
import './QuickFilterModal.scss'

export const QuickFilterModal = observer(() => {
  return (
    <div>TODO: Quick Filter Modal</div>
  )
});